spring.application.name=demo

# PostgreSQL (Docker ? Docker)
spring.datasource.url=jdbc:postgresql://postgres:5432/dailyrecords
spring.datasource.username=postgres
spring.datasource.password=admin
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Disable H2 (you're not using it)
spring.h2.console.enabled=false

# JWT config
jwt.secret=
jwt.expiration-ms=86400000

# Email Configuration
# For Gmail SMTP (you'll need to use an App Password, not your regular password)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=shrutibilolikar2003@gmail.com
spring.mail.password=
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Frontend URL for email links
app.frontend.url=http://localhost:5173
